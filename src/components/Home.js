import React from "react";

const Home = () => (
  <div>
    This example explains the code and performance differences between Hooks and
    classic class components when using async calls
  </div>
);

export default Home;
